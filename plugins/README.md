# Nano-Editor Python Bindings

This library is the base bindings between the Cython implementation and the Python plugins layer. Install this using: `python3 setup.py install` to enable the python bindings.
